# 安全策略

本文说明 FATHOM 的安全边界、凭证处理方式，以及如何报告漏洞。

## 安全边界

FATHOM 面向企业内网部署，运行时的安全假设是：**访问者已具备网络入口，但未经过授权**。
因此所有对外部数据的执行都必须经过以下三道控制，任何一道都不能被绕过。

### 1. 只读执行

- 物理计划器（`apps/api/src/fathom/application/physical_planner.py`）只编译参数化只读 SQL，
  由已评审的物理映射生成，不接受外部传入的 SQL 文本，不接受字符串拼接。
- 连接器只接受由物理计划器生成的只读 `SELECT`，其他语句在入口处被拒绝：
  `apps/api/src/fathom/application/connector_executor.py` 中的校验会直接抛出
  `连接器仅接受由物理计划器生成的只读 SELECT`。
- 平台内的 SQL 模板同样限制为只读 `SELECT`/CTE。
- 查询结果集有行数上限与超时上限（默认 1000 行 / 8 秒），由策略执行点作为义务下发。

FATHOM 不提供数据写入、DDL 或存储过程调用能力。若你的场景需要写操作，请不要通过修改连接器绕过，
这超出当前设计边界。

### 2. 策略强制点

- 统一策略执行点 `PolicyEnforcementPoint` 位于 `apps/api/src/fathom/application/policy_enforcement.py`。
  每次运行产生一条 `PolicyDecision`，包含主体、角色、动作、资源、对象范围、允许/拒绝、义务与原因。
- HTTP 中间件的鉴权（`apps/api/src/fathom/application/security.py`）负责入口身份与角色、
  对象范围控制；策略执行点负责领域级运行授权。两者不可互相替代——入口鉴权通过不代表领域动作被授权，
  内部调用也不能因为「来自本进程」而豁免。
- 对象范围被下推为绑定谓词，越权对象在执行前被拒绝，拒绝原因与决策 ID 会随错误返回，便于审计。
- 能力调用继承能力定义的最小角色与副作用等级；有副作用的动作默认需要批准令牌，未批准不得执行。
- MCP 与 A2A 入口共用同一套查询与连接器限制，不存在绕过路径。

设计依据见 `docs/adr/0002-central-policy-enforcement.md`。后续如需替换为外部 PDP（如 OPA），
应保持 `PolicyEnforcementPoint` 的调用契约不变，而不是在业务调用方散落权限判断。

### 3. 执行收据

- 每次外部查询、能力调用与动作尝试都会写入不可变的 `ExecutionReceipt`
  （`apps/api/src/fathom/application/semantic_runtime.py`），追加写、不原地篡改。
- 收据记录逻辑计划哈希、脱敏后的物理计划、映射/策略/能力版本、主体、状态、耗时、行数、
  结果哈希、数据新鲜度、质量结果、错误与父级 trace，并对齐 OpenLineage 的运行/作业/数据集概念。
- 敏感配置与凭证永不进入收据。收据用于回放、对账、审计与影响分析，
  也是判断「某次答案用了哪个口径与哪份数据」的唯一依据。

设计依据见 `docs/adr/0003-execution-receipts-and-open-lineage.md`。

### 其他边界

- 语义契约、黄金问题集与示例数据集是仓库内的示例资产，会随代码一同公开，不应放入任何真实业务数据。
- `config/fathom.example.yaml` 与 `config/fathom.production.example.yaml` 仅含占位值，
  可安全提交；实际运行配置 `config/fathom.yaml` 与 `data/` 下的本地数据库不进入仓库。
- 前端对模型与知识库返回的富文本做净化后再渲染，避免注入。

## 凭证处理

**凭证只通过环境变量或 `secret_reference` 提供，绝不写入数据库、配置文件或执行收据。**

- 环境变量：在启动进程前注入，例如 PostgreSQL 演示运行时使用
  `$env:FATHOM_DEMO_PG_PASSWORD = '<your-password>'`，同名变量在服务启动时必须继续提供。
- `secret_reference`：在配置里只写引用而非值，例如
  `secret_reference: env://FATHOM_LOCAL_MODEL_API_KEY`
  （见 `config/fathom.example.yaml`）。运行时按引用解析真实密钥，引用字符串本身可以安全提交与展示。
- 数据源、模型网关与知识库连接统一遵循上述两种方式，管理接口不会回显密钥明文。
- API 令牌以 SHA-256 摘要形式配置，可用 `fathom token-hash <token>` 生成，
  比较时使用恒定时间比较，避免时序侧信道。
- 收据与审计记录中的凭证字段按脱敏规则处理；物理计划入收据前同样脱敏。

如果你在提交中发现疑似凭证：

1. 不要开公开 issue，也不要先提交删除——公开仓库的历史仍会保留该值。
2. 立即按下方渠道私下告知我们，并在你的环境里轮换该凭证。
3. 仅在凭证轮换完成后，我们才会协助清理相关历史内容。

## 报告漏洞

请发送邮件至 **security@example.com**（占位邮箱，正式对外前会替换为真实联系地址）。

邮件请包含：

- 受影响的版本或提交哈希；
- 漏洞类型与影响面（是否可越权查询对象、是否可绕过只读限制、是否可读取凭证）；
- 可复现的最小步骤或请求样例；
- 你已验证的影响证据（可打码，但请保留关键结构）；
- 你希望的披露方式与时间。

我们的处理流程：

- 3 个工作日内确认收到；
- 10 个工作日内给出初步评估与后续计划；
- 修复发布后，经你同意会在 CHANGELOG 中致谢。

**请不要通过公开 issue、讨论区或 PR 报告安全问题。** 在修复发布前，我们请求你保留披露细节。
我们也接受「先修复后公开」的协调披露，具体节奏以你不受风险为准。

## 支持版本

| 版本 | 安全更新 |
|---|---|
| 0.1.x | 支持 |
| 更早的自定义分支或衍生版本 | 不支持，请合并上游修复 |

当前处于开源准备阶段，仅 `0.1.x` 系列接收安全修复。修复会进入 `master` 并在后续补丁版本发布；
请以 CHANGELOG 的 `Unreleased` 与版本小节为准，不要依赖内部渠道。